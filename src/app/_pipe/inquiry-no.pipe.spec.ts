import { InquiryNoPipe } from './inquiry-no.pipe';

describe('InquiryNoPipe', () => {
  it('create an instance', () => {
    const pipe = new InquiryNoPipe();
    expect(pipe).toBeTruthy();
  });
});
